{
  "buildCommand": "echo 'No build needed - vanilla HTML + serverless'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/server.js"
    }
  ],
  "crons": [
    {
      "path": "/api/cron-refresh",
      "schedule": "0 0 * * *"
    }
  ]
}
